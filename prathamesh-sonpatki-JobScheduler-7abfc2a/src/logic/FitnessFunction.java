package logic;

/*
* To change this template, choose Tools | Templates
* and open the template in the editor.
* returns true of found the solution
 */

/**
 *
 * @author Pawan
 */
public class FitnessFunction {

    /*
     *   boolean findNEPoint(Population population)
     * {
     *     boolean NEPoint = false;
     *     for(int i=0;i<population.populationSize;i++)
     *     {
     *         if(population.getChromosomes()[i].getFitnessValue() == 0)//reached NE point of game
     *         {
     *             NEPoint = true;
     *         }
     *     }
     *     if(NEPoint == true)
     *     {
     *         return true;
     *     }
     *     return false;
     * }
     *
     */
}


//~ Formatted by Jindent --- http://www.jindent.com
